const { test, expect } = require('@playwright/test');

test.describe('Add Paygrades Feature', () => {
    test('should allow user to add a new paygrade', async ({ page }) => {
   
        
    });
});